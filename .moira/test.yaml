name: test

trigger:
  on_push:
    branches: [main]
  on_pr:
    target: [main]

steps:
  - name: test
    env: .moira/envs/rust.nix
    run: |
      rustup default nightly
      cargo check
      cargo test
    vars:
      CARGO_TERM_COLOR: always
